# ─────────────────────────────────────────────────────────────────────────────
# MySQL Docker — environment variable template
#
# Copy this file to .env and fill in real values BEFORE running docker compose.
#
#   cp .env.example .env
#   nano .env        # or your preferred editor
#
# NEVER commit .env to git. It is listed in .gitignore.
# ─────────────────────────────────────────────────────────────────────────────

# MySQL root password — use a long, random string (≥ 32 chars recommended).
# Example generation: openssl rand -base64 32
MYSQL_ROOT_PASSWORD=change-me-to-a-strong-random-password

# Host port that MySQL is mapped to on 127.0.0.1.
# Change only if 3306 is already in use on this machine.
MYSQL_PORT=3306

